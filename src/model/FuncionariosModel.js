export default class FuncionariosModel {
    constructor(id, CPF, nome, cargo, salario, situacao) {
            this.id = id,
            this.cpf = CPF,
            this.nome = nome,
            this.cargo = cargo,
            this.salario = salario,
            this.situacao = situacao
    }
}